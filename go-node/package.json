{
  "name": "node-golang-wasm-example",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:go": "cd ../go && GOOS=js GOARCH=wasm go build -o main.wasm main.go",
    "build": "npm run build:go",
    "start": "npm run build && node src/index.js",
    "dev": "npm run start",
    "clean": "rm -f ../go/main.wasm"
  },
  "keywords": ["node", "wasm", "golang"],
  "author": "Sam Duffield",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
